<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>Gastty</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIconFile</key>
    <string>AppIcon</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.developer-tools</string>
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
    <key>NSHighResolutionCapable</key>
    <true/>
    <key>NSHumanReadableCopyright</key>
    <string>© 2026 Hampus Åberg</string>
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>
    <!-- ===== Sparkle (auto-update) ===== -->
    <!-- Appcast feed Sparkle polls for new versions. The release
         workflow rewrites this file on every tag push and commits it
         to /docs so GitHub Pages serves it at this URL. -->
    <key>SUFeedURL</key>
    <string>https://hampusaberg.github.io/Gastty/appcast.xml</string>
    <!-- EdDSA public key used to verify update bundles. Replace the
         placeholder below with the output of `generate_keys` (the
         Sparkle CLI tool). The matching private key never leaves your
         Mac / repo secrets. -->
    <key>SUPublicEDKey</key>
    <string>4BsXElSusKTtCTxwAy2maGedHudLNaTzoiGCzZ4Y/PU=</string>
    <!-- Auto-check by default; users can disable from the in-app
         update prompt's checkbox. Sparkle polls every 24h. -->
    <key>SUEnableAutomaticChecks</key>
    <true/>
</dict>
</plist>
